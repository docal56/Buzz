import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Buzz | Property issue intake",
  description:
    "Buzz turns tenant calls into clear property issues and call monitoring for property teams.",
};

export default function MarketingPage() {
  return <main className="min-h-full" />;
}
